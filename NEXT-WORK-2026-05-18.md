# Next work — scoped tasks (2026-05-18)

Two real problems remain after this session. Both are scoped tasks
with a plan below — **not** live trial-and-error at 20–30s/step.

## Session context (what's now known/true)

- **Grounding is ~100% / ~5px.** The "~25% precision floor" that
  gated the project was a broken `vision-precision.ts` oracle. Fixed
  in `803e774` (`keypadGrid`, AX-measured proportions). Modal Holo3
  grounds Calculator buttons to ~5px. Do not re-litigate this.
- **`refine`/upscale (eyes.ts) is a dead end** — 0/8 even when base
  is 100%. Kept default-off as a preserved experiment.
- **INFEASIBLE outcome** wired + typechecked (`53cb980`); untestable
  via t5 until the brain can navigate (see Item 2).
- **Completion probe** committed (`8ae72f3`): periodic skeptical
  verifier so a "done but won't stop" run terminates. Safe, env-
  guarded (`PONDER_COMPLETION_PROBE`), observed not false-positiving;
  positive case unvalidated (runs cancelled before completing).
- Provider preference is currently `remote` (Modal) in
  `~/.holo3-agent/preferences.json` (backup `.bak` = original
  `hcompany`). Modal is ~10–11s/plan + ~10–11s/ground on the full
  1512×982 frame → ~20–30s/step. That latency is what makes the loop
  un-iterable and is the reason Item 1 is the dependency for Item 2.

---

## Item 1 — Native-resolution crop (SPEED). Do this first.

### Problem
`maybeCropToTargetApp` (`src/agent/loop.ts:262`) crops the target
window at **logical resolution** (`scaleFactor=1`): for Calculator
it emits a 230×408 ≈ 8 KB thumbnail (dev log: `→8447 bytes, ~15.8×
fewer pixels`). The model grounds that degraded image wrong ~100% of
the time (observed: "4" → (114,399)). So the `MIN_CROP_DIM_PX = 300`
guard (`loop.ts:~334`) correctly forces sub-300px windows to run
**uncropped** — i.e. the full 1512×982 / ~1.2 MB frame every step,
which is the ~10s/call Modal bottleneck.

`bench/vision-precision.ts` proves the fix: its `cropPng` crops the
**native/Retina PNG** (460×816 for the same window) and grounds at
**8/8, ~5px**. Native-res crop is both accurate AND far fewer pixels
than full-screen → the real speedup.

### Fix
Make `maybeCropToTargetApp` crop from the **native-resolution**
screenshot, not a logical-downsampled one. Mirror the proven math in
`bench/vision-precision.ts` (~lines 300–345):

1. Read PNG IHDR dims from `shot.png`; `scaleX = pngW / shot.width`,
   `scaleY = pngH / shot.height` (2 on Retina).
2. Crop rect in **PNG-pixel space**:
   `x=(bounds.x-shot.offsetX)*scaleX`, `y=…*scaleY`,
   `w=bounds.width*scaleX`, `h=bounds.height*scaleY`.
3. Produce the crop **without downscaling** (keep native px). If
   `nativeImage` is downsampling, switch to the bench's `sips`
   approach (`src/agent/imageops.ts cropAndScalePng` with scale=1
   already does an exact native crop — reuse it).
4. Keep `screen=[bounds.width, bounds.height]` (logical) as the
   grounding coord space; the loop's existing offset translation
   (`coords + shot.offsetX/Y`, plus `+bounds.x/y` for the crop
   origin) maps back. Verify the translation matches the bench's
   `r.x + bounds.x` exactly.
5. Only AFTER native-res crop is confirmed: lower/remove
   `MIN_CROP_DIM_PX` (env: `PONDER_MIN_CROP_DIM_PX`). Keep a tiny
   sanity floor (~80px) + the existing multi-monitor recapture and
   off-screen-fit checks (orthogonal, keep them).

### Validation (offline, fast — no 20s/step loop)
- `bench/vision-precision.ts` `cropped` column must stay 8/8 ~5px
  (it already simulates exactly native-res crop).
- One `calculator-mouse-math` run: dev log must show
  `🪟 cropped to Calculator … scaleFactor=2 … native` and per-step
  `ground (…)` ≈ 2–4s (down from ~10s). Window must be a normal
  size; reset Calculator first.

### Scope / risk
~40–80 LOC in `maybeCropToTargetApp`; reuse `imageops.cropAndScalePng`
+ the bench's scale math (both already proven). Risk low — it's a
resolution fix to an existing path, validated by an existing offline
bench. Est: ~half a focused session.

---

## Item 2 — Brain decomposition / sequencing (STABILITY).

### Problem
Flat `agent_do` skips the planner (`runTask` flat branch,
`loop.ts:~604`; the Ollama planner was disabled because it
over-decomposed — rationale at `loop.ts:~586`). Holo3 is
grounding-first: it executes single clicks accurately but (a) never
emits DONE on a multi-step task, and (b) mis-sequences (observed:
double-entered "4", looped `8,=,8,=`, never reached 47×8). The
committed completion probe mitigates (a); (b) is unsolved and is the
real "make the app stable" problem.

### Root cause
No task-state tracking. Each step the brain re-plans from the
screenshot + a flat history list; it has no notion of "which
sub-step am I on / what did I already do / am I done." Holo3 is not
a planner and prompt-engineering termination into the small model is
documented as fragile (`brain.ts` preamble comments).

### Options (this is a DESIGN task — evaluate, don't blind-code)
- **A. One-shot strong-model decomposition (recommended to
  prototype).** At task start, a single call to the hosted model
  (Holo3/H or a focused prompt) → ordered atomic checklist. Drive the
  loop one item at a time; advance only when that item's goal is
  verified (reuse `verifier.ts`). This is *not* the old Ollama
  planner (that over-decomposed *per nothing*); it's one strong
  decomposition + explicit position tracking. Gate behind an env
  flag for multi-step tiers; keep flat mode the default for true
  single-step `agent_do`.
- **B. Structured progress memory in the flat prompt.** Cheaper,
  but the small model drops structure (documented) — likely fragile.
- **C. Deterministic per-domain done/next signals** where cheap
  (e.g. read Calculator display via AX → know expression entered →
  press `=` → read result → DONE). Very robust where available,
  generalizes poorly; pairs with the completion probe.

### Plan
1. **Depends on Item 1** (un-iterable at 20–30s/step; native crop →
   ~3–5s/step makes A developable/measurable).
2. Write a short design doc choosing A (with C as a robustness
   pairing for native apps). Define the checklist schema, the
   advance/verify contract, and the env flag.
3. Prototype A behind the flag; validate on `calculator-mouse-math`
   then a T4 case via `bench/run-all.ts`.
4. Re-validate the completion probe's positive case in the same
   pass (runs will now complete).

### Scope
Multi-session. First deliverable = design doc + flagged Option-A
prototype. Not a one-patch fix; do not attempt under time pressure.

---

## Order of operations

1. **Item 1** (native-res crop) — unblocks iteration speed. ~½ session.
2. **Item 2** (decomposition) — the real stability work; needs Item 1.
3. Re-validate completion probe + INFEASIBLE/t5 once Item 2 lets the
   agent actually reach those states.

Do not run live agent tasks at 20–30s/step to "debug" — fix Item 1
first so the loop is fast enough to iterate honestly.
