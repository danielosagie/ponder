<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Hardened Runtime entitlements required for the Ponder desktop app.

    Electron itself needs allow-jit + allow-unsigned-executable-memory
    because V8 generates code at runtime. disable-library-validation lets
    nut-js load its native .node binary (different code signature than
    Ponder.app's). apple-events lets the agent script other apps via
    AppleScript when a task requires it (e.g., "open Notes").

    Privacy permissions (Accessibility, Screen Recording, Input Monitoring)
    are NOT entitlements — those are runtime-prompted privacy requests the
    user grants in System Settings. The matching usage strings live in
    electron-builder.yml's mac.extendInfo.
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.automation.apple-events</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>
